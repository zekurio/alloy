{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connect",
  "description": "Native login runs only in the bundled connect window. Saved servers can also be listed, switched, and forgotten from the selected server's web UI through the desktop bridge.",
  "windows": ["connect"],
  "permissions": [
    "allow-connect-server",
    "allow-connect-ready",
    "allow-cancel-connect",
    "allow-saved-servers",
    "allow-forget-server"
  ]
}
