{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connect",
  "description": "Only the bundled connect window can run native login and the local saved-server commands.",
  "windows": ["connect"],
  "permissions": [
    "allow-connect-server",
    "allow-connect-ready",
    "allow-cancel-connect",
    "allow-saved-servers",
    "allow-forget-server"
  ]
}
