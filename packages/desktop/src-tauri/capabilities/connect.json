{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connect",
  "description": "Only the bundled connect window can manage native login and saved servers.",
  "windows": ["connect"],
  "permissions": [
    "allow-connect-server",
    "allow-connect-ready",
    "allow-cancel-connect",
    "allow-saved-servers",
    "allow-forget-server"
  ]
}
