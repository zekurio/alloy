# Pin devenv's nixpkgs to the same channel flake.nix tracks so local Node/pnpm
# and packaging stay aligned. Bump together with `nix flake update`.
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
