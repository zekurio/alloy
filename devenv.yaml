# Pin devenv's nixpkgs to the same channel flake.nix tracks so the dev Deno
# stays aligned with the packaged/CI Deno. Bump together with `nix flake update`
# (and re-check the denort pin in nix/package.nix).
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
