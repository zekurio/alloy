# ─────────────────────────────────────────────────────────────
# Web service env
# Copy to .env (dev) or paste into Railway variables (prod).
# ─────────────────────────────────────────────────────────────

# Public origin of the Alloy server. Browser code calls this URL directly.
# Do not include a trailing `/api`.
VITE_SERVER_URL=http://localhost:3000
